import { Meta, Source } from "@storybook/blocks";

<Meta title="Tokens/Properties" />

## CSS Properties

The @property CSS at-rule is part of the CSS Houdini set of APIs. It allows developers to explicitly define CSS custom properties, allowing for property type checking and constraining, setting default values, and defining whether a custom property can inherit values or not.

### HENK Properties

<Source
    language="css"
    dark={true}
    code={`
\@property --size-width-content {
  syntax: "<length>";
  inherits: true;
  initial-value: 1140px;
}
`}
/>

<Source
    language="css"
    dark={true}
    code={`
:root {
  --size-width-content: 1140px;
}
`}
/>

<Source
    language="css"
    dark={true}
    code={`
\@property --line-clamp {
  syntax: "<integer>";
  inherits: true;
  initial-value: 3;
}
`}
/>

<Source
    language="css"
    dark={true}
    code={`
.truncate {
  display: -webkit-box;
  -webkit-line-clamp: var(--line-clamp);
  -webkit-box-orient: vertical;
  overflow: hidden;
}
`}
/>
