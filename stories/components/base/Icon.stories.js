import { fn } from '@storybook/test';
import { icons } from './icons';

export default {
  title: 'Components/Base/Icon',
  tags: ['autodocs'],
};

export const Share = () => icons['icon-share'];
export const Truck = () => icons['icon-truck'];
export const Checkmark = () => icons['icon-checkmark'];
